using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonSpecialClickSound : MonoBehaviour
{
    [Header("用于播放非默认音效 音效id default默认音效 none为不播放音效 其它为自定义音效")]
    public string soundId = "default";
}